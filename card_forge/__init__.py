"""card-forge — generate and diff Hugging Face model cards from facts.

Deterministic, stdlib-only: point it at a model config (or a bundled
fixture, or a live Hub repo id) and get a complete, honest model card with
a checklist score — plus config diffing to see what changed between
revisions.
"""

__version__ = "0.1.0"